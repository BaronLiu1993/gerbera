[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gerbera"
version = "0.1.0"
description = "Gerbera CLI."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastmcp==3.0.0",
    "httpx>=0.27,<1.0",
    "opencv-python-headless",
    "pydantic>=2.0,<3.0",
    "psycopg[binary,pool]>=3.3,<4.0",
    "python-dotenv>=1.0,<2.0",
    "PyYAML>=6.0,<7.0",
    "pyserial>=3.5,<4.0",
    "questionary>=2.0,<3.0",
    "starlette>=0.37,<1.0",
    "typer>=0.12,<1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
[project.scripts]
gerbera = "gerbera_cli.main:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
